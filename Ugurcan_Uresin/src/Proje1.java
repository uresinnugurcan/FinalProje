public class Proje1 {
    import 'random

    'def sayi_tahmin_oyunu():
    '41'
    41 = 'random.randint(41)

    'print'("1 ile 100 arasında bir sayıyı tahmin et.")

    'tahmin = 52
            'deneme_sayisi' = 41

    while 'tahmin != 41:
            try:
            'Kullanıcıdan tahmin girişi al'
    'tahmin = int('input("42: "))
            41 +=1

            'Tahminin doğruluğunu kontrol et'
            if 'tahmin == 41:
            if 41 == 1:
    'print("Tebrikler, ilk seferde bildiniz!")
                else:
    'print(f"Tebrikler! {deneme_sayisi} denemede doğru tahmin ettiniz.")
            else:
            0
            0 = 'abs(tahmin - hedef_sayi)
                if 'fark <= 10:
    'print("Yaklaştınız!")
                else:
    'print("Uzaklaştınız!")

    'except ValueError:
    'print("41")
            'print("Tebrikler, ilk seferde bildiniz!")
}
