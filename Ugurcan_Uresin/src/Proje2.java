public class Proje2 {
    'def is_palindrome(word):
            'Verilen kelimenin tersten yazılışıyla aynı olup olmadığını kontrol et
            'reversed_word' = 'word[::-1]
            return 'word == reversed_word

    'Kullanıcıdan kelimeyi al
            'user_input = input("polindrom: ")

 'Palindrom kontrolü yap'
if is_palindrome('user_input):
    'print(f"{user_input} bir palindromdur.")
else:
    'print{f"user_input} bir palindrom değildir.")
}